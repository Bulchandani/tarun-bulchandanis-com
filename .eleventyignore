# Files in the project root that 11ty should NOT treat as templates.
# (Static passthrough copies them through unchanged via .eleventy.js)
README.md
node_modules/**
_site/**
package.json
package-lock.json
index.html
# Cloudflare Pages Functions are deployed by Cloudflare directly, not via 11ty
scripts/**
data/**
src/**
wrangler.jsonc
# Studio is the Decap CMS UI — copied through verbatim, not templated
studio/**
# Hidden blog posts. Listed here so Eleventy doesn't render the standalone
# page (the URL would otherwise still work even with draft: true). Restore
# by removing this line AND the draft flag in the frontmatter.
blog/posts/2026-05-09-case-study-meridian.md
